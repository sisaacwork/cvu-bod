# ─────────────────────────────────────────────────────────────────────────────
# CVU BoD Title Finder — Streamlit Secrets
# ─────────────────────────────────────────────────────────────────────────────
# 1. Copy this file: cp .streamlit/secrets.toml.example .streamlit/secrets.toml
# 2. Fill in the values below with your real credentials.
# 3. NEVER commit secrets.toml to Git — it is already in .gitignore.
# ─────────────────────────────────────────────────────────────────────────────

[mysql]
host     = "mysql.ctbuh.org"       # e.g. "db.example.com" or "127.0.0.1"
port     = 3306                  # default MySQL port; change if needed
user     = "build_db_prod_RO"
password = "pR=c8E&RuB9Y"
database = "buldingdb"

# Path to UsersExport.csv.
# If the CSV lives right next to app.py you can leave this out entirely —
# the app will find it automatically.
# Otherwise set an absolute path, e.g.:
#   csv_path = "/Users/you/Documents/GitHub/cvu-bod/UsersExport.csv"
# csv_path = "UsersExport.csv"
